package Set_HashSet;

public class LabraDog extends Dog {

    public LabraDog(String name) {
        super(name);
    }

   /* @Override
    public boolean equals(Object obj) {
        if (this==obj){
            return true;
        }

        if (obj instanceof  LabraDog){
            String objName=((LabraDog) obj).getName();
            return this.getName().equals(objName);
        }
        return false;
    }*/
}
